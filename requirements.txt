# SQLPilot 共享依赖：Linux + Python 3.11。
#
# PyTorch 必须先按 GPU 单独安装，二选一：
# V100 (FP16, CUDA 12.6):
#   pip install torch==2.11.0+cu126 --index-url https://download.pytorch.org/whl/cu126
# RTX 4090 (BF16 + TF32, CUDA 12.8):
#   pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#
# 然后安装本文件：pip install -r requirements.txt
transformers==5.14.1
huggingface-hub==1.24.0
datasets==5.0.0
accelerate==1.14.0
peft==0.19.1
trl==1.8.0
bitsandbytes==0.50.0
sentencepiece==0.2.1
PyYAML>=6.0.1,<7
tensorboard>=2.18,<3
sqlparse>=0.5,<1
nltk>=3.9,<4
pytest>=8,<9
